-- It will occur subtransaction overflow when insert data to segments 1000 times.
-- All segments occur overflow.
DROP TABLE IF EXISTS t_zjp_1;
NOTICE:  table "t_zjp_1" does not exist, skipping
CREATE TABLE t_zjp_1(c1 int) DISTRIBUTED BY (c1);
CREATE OR REPLACE FUNCTION transaction_test0()
RETURNS void AS $$
DECLARE
    i int;
BEGIN
	FOR i in 0..1000
	LOOP
		BEGIN
			INSERT INTO t_zjp_1 VALUES(i);
		EXCEPTION
		WHEN UNIQUE_VIOLATION THEN
			NULL;
		END;
	END LOOP;
END;
$$
LANGUAGE plpgsql;
-- It will occur subtransaction overflow when insert data to segments 1000 times.
-- All segments occur overflow.
DROP TABLE IF EXISTS t_zjp_2;
NOTICE:  table "t_zjp_2" does not exist, skipping
CREATE TABLE t_zjp_2(c int PRIMARY KEY);
CREATE OR REPLACE FUNCTION transaction_test1()
RETURNS void AS $$
DECLARE i int;
BEGIN
	for i in 0..1000
	LOOP
		BEGIN
			INSERT INTO t_zjp_2 VALUES(i);
		EXCEPTION
			WHEN UNIQUE_VIOLATION THEN
				NULL;
		END;
	END LOOP;
END;
$$
LANGUAGE plpgsql;
-- It occur subtransaction overflow for coordinator and all segments.
CREATE OR REPLACE FUNCTION transaction_test2()
RETURNS void AS $$
DECLARE
    i int;
BEGIN
	for i in 0..1000
	LOOP
		BEGIN
			CREATE TEMP TABLE tmptab(c int) DISTRIBUTED BY (c);
			DROP TABLE tmptab;
		EXCEPTION
			WHEN others THEN
				NULL;
		END;
	END LOOP;
END;
$$
LANGUAGE plpgsql;
SET gp_log_suboverflow_statement = ON;
BEGIN;
SELECT transaction_test0();
 transaction_test0 
-------------------
 
(1 row)

SELECT count(*) FROM (SELECT (i).segid, (i).query, (i).subxact_overflowed, (i).sess_id, (i).pid FROM (SELECT gp_suboverflowed_backends())a(i) ORDER BY (i).segid) AS test;
 count 
-------
     3
(1 row)

SELECT DISTINCT logsegment, logmessage FROM gp_toolkit.gp_log_system
	WHERE logdebug = 'INSERT INTO t_zjp_1 VALUES(i)'
	ORDER BY logsegment;
 logsegment |                         logmessage                          
------------+-------------------------------------------------------------
 seg0       | Statement caused suboverflow: INSERT INTO t_zjp_1 VALUES(i)
 seg1       | Statement caused suboverflow: INSERT INTO t_zjp_1 VALUES(i)
 seg2       | Statement caused suboverflow: INSERT INTO t_zjp_1 VALUES(i)
(3 rows)

COMMIT;
BEGIN;
SELECT transaction_test1();
 transaction_test1 
-------------------
 
(1 row)

SELECT count(*) FROM (SELECT (i).segid, (i).query, (i).subxact_overflowed, (i).sess_id, (i).pid FROM (SELECT gp_suboverflowed_backends())a(i) ORDER BY (i).segid) AS test;
 count 
-------
     3
(1 row)

SELECT DISTINCT logsegment, logmessage FROM gp_toolkit.gp_log_system
	WHERE logdebug = 'INSERT INTO t_zjp_2 VALUES(i)'
	ORDER BY logsegment;
 logsegment |                         logmessage                          
------------+-------------------------------------------------------------
 seg0       | Statement caused suboverflow: INSERT INTO t_zjp_2 VALUES(i)
 seg1       | Statement caused suboverflow: INSERT INTO t_zjp_2 VALUES(i)
 seg2       | Statement caused suboverflow: INSERT INTO t_zjp_2 VALUES(i)
(3 rows)

COMMIT;
BEGIN;
SELECT transaction_test2();
 transaction_test2 
-------------------
 
(1 row)

SELECT count(*) FROM (SELECT (i).segid, (i).query, (i).subxact_overflowed, (i).sess_id, (i).pid FROM (SELECT gp_suboverflowed_backends())a(i) ORDER BY (i).segid) AS test;
 count 
-------
     4
(1 row)

SELECT DISTINCT logsegment, logmessage FROM gp_toolkit.gp_log_system
	WHERE logmessage = 'Statement caused suboverflow: SELECT transaction_test2();'
	ORDER BY logsegment;
 logsegment |                        logmessage                         
------------+-----------------------------------------------------------
 seg-1      | Statement caused suboverflow: SELECT transaction_test2();
 seg0       | Statement caused suboverflow: SELECT transaction_test2();
 seg1       | Statement caused suboverflow: SELECT transaction_test2();
 seg2       | Statement caused suboverflow: SELECT transaction_test2();
(4 rows)

COMMIT;
SET gp_log_suboverflow_statement = OFF;
